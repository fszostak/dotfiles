- clean: ['~']
- link:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.Xresources:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.Xmodmap:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
