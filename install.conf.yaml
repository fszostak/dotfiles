- clean: ['~']

- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.Xresources:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.i3/config: i3/config
    ~/.config/twmn/twmn.conf: twmn.conf

- shell:
  - [git submodule update --init --recursive, Installing submodules]
